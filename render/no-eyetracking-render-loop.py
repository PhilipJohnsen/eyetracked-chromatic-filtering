"""no-eyetracking-render-loop.py
rendering loop with NO eye tracking:
- No maskin
- Gaussian filter of Swiatzcak and Shaeffel on all pixels

Notes for this:
edges are calculated as repeating the edge pixel
the kernel used can be either gaussian or circular

Process:
  - Import files and libraries
  - Load settings into memory
  - Initialize frame capture
  - Initialize render_blur.py
  - Initialize rendering loop
      - Capture a frame
      - Blur the frame
      - Display the frame
      - Option to quit
      - Telemetry / debug prints
  - Cleanup
"""


#Process / list of actions
  #Import files and libraries
import json
import time
from pathlib import Path
import numpy as np
from render_blur import

  #Load settings into memory

  #Initialize frame capture

  #Initialize render_blur.py

  #Initialize rendering loop
    #Capture a frame

    #Blur the frame

    #display the frame

    #option for quitting the rendering loop

    #telemetry / debug prints

  #cleanup when quitting


